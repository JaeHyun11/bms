<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin.goods">
	
	<select id="selectListGoods" resultType="GoodsDto" >
		SELECT 
	   			*
	    FROM 
				GOODS 	            		       
	</select>
	
	<insert id="insertGoods" parameterType="GoodsDto">
		    INSERT INTO 
		  			GOODS (
						GOODS_NM,
					    GOODS_GROUP,
						GOODS_CATEGORY,
						GOODS_COLOR,
						SORT,
						GOODS_INFO,
						GOODS_FILE_NAME,
						PRICE,
						DISCOUNT_RATE,
						XS_STOCK,
						SM_STOCK,
						MD_STOCK,
						LG_STOCK,
						XL_STOCK,
						POINT,
						DELIVERY_PRICE,
						ENROLL_DT				  			
		  			)
		    VALUES (
			        #{goodsNm},
				    #{goodsGroup},
				    #{goodsCategory},
				    #{goodsColor},
				    #{sort},
				    #{goodsInfo},
				    #{goodsFileName},
				    #{price},
				    #{discountRate},
					#{xsStock},
					#{smStock},
					#{mdStock},
					#{lgStock},
					#{xlStock},
					#{point},
					#{deliveryPrice},
					NOW()
 			 )
	</insert> 
	
	<update id="updateGoods" parameterType="GoodsDto" >
	   UPDATE 
	   			GOODS
	   <set> 
	      <if test='goodsNm != null and goodsNm != ""'>
	        GOODS_NM = #{goodsNm},
	      </if>
	      <if test='goodsGroup != null and goodsGroup != ""'>
	        GOODS_GROUP = #{goodsGroup},
	      </if>
	      <if test='goodsCategory != null and goodsCategory != ""'>
	        GOODS_CATEGORY = #{goodsCategory},
	      </if>
	      <if test='goodsColor != null and goodsColor != ""'>
	        GOODS_COLOR = #{goodsColor},
	      </if>
	      <if test='sort != null and sort != ""'>
	        SORT = #{sort},
	      </if>
	      <if test='goodsInfo != null and goodsInfo != ""'>
	        GOODS_INFO = #{goodsInfo},
	      </if>
	      <if test='goodsFileName != null and goodsFileName != ""'>
	        GOODS_FILE_NAME = #{goodsFileName},
	      </if>
	      <if test='price != null and price != ""'>
	        PRICE = #{price},
	      </if>
	      <if test='discountRate != null and discountRate != ""'>
	        DISCOUNT_RATE = #{discountRate},
	      </if>
	      <if test='xsStock != null and xsStock != ""'>
	        XS_STOCK = #{xsStock},
	      </if>
	      <if test='smStock != null and smStock != ""'>
	        SM_STOCK = #{smStock},
	      </if>
	      <if test='mdStock != null and mdStock != ""'>
	        MD_STOCK = #{mdStock},
	      </if>
	      <if test='lgStock != null and lgStock != ""'>
	        LG_STOCK = #{lgStock},
	      </if>
	      <if test='xlStock != null and xlStock != ""'>
	        XL_STOCK = #{xlStock},
	      </if>
	      <if test='point != null and point != ""'>
	        POINT = #{point},
	      </if>
	      <if test='deliveryPrice != null and deliveryPrice != ""'>
	        DELIVERY_PRICE = #{deliveryPrice},
	      </if>
	    </set>
		WHERE 
				GOODS_CD = #{goodsCd}
	</update>	
		
	<delete id="deleteGoods" parameterType="int">
		DELETE FROM GOODS WHERE GOODS_CD = #{goodsCd}
	</delete>	
		
</mapper>
